#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 *_atoi - function that converts a string of digits
 *@s: pointer to the string to be converted
 * Return: 0
 */
int _atoi(char *s)
{
	return (atoi(s));
}

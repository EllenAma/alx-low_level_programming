#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isalpha - function to check whether the input integer represents
 * an alphabetic character or not
 * @c: input argument
 * Return: 0
 */
int _isalpha(int c)
{
	return (isalpha(c));
}

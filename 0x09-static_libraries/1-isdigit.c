#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isdigit - function that takes an integer argument and
 * returns integer
 * @c: integer argument
 * Return: 0
 */
int _isdigit(int c)
{
	return (isdigit(c));
}

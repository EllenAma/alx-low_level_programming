#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _puts - function that prints a string of characters to standard
 * output
 * @s: character pointer that points to the beginning of
 * the string to be printed
 * Return: void
 */
void _puts(char *s)
{
	puts(s);
}
